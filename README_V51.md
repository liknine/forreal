# ForReal v51 real products fix

Заменить только `index.html`, `style.css`, `app.js`.

Не заменять `data/products.json`, `images/products/`, `bot/`, `bot/.env`.

Что исправлено:
- тестовые 2 товара убраны;
- каталог показывает только товары из `data/products.json`;
- если товаров нет или файл не загрузился, тестовые товары не появляются;
- динамические карточки товара кликабельны;
- размеры берутся из `sizeStock`;
- размеры с остатком 0 disabled;
- корзина проверяет актуальные остатки;
- самовывоз открывает @woodyqqqq с готовым текстом заказа и ссылками на фото;
- тестовый заказ в MY ORDERS убран.

Важно: если товар добавлен ботом локально, он появится на сайте GitHub Pages только после синхронизации `data/products.json` и `images/products/` в GitHub.
